# kind 叢集設定：把主機的 30080 對應到 NodePort 30080，瀏覽器直接打 http://localhost:30080
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: harness-lab
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080
        hostPort: 30080
        protocol: TCP
