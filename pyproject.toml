[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["app/legacy_report.py"]  # 練習用檔案，刻意不 lint

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
